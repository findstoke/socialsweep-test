# socialsweep-test

Information: Joshua Billington | 778-906-4176 | joshuambillington@gmail.com

The project contains shared utils in /utils, shared types in /types, and tests in /tests. 

Leveraging vitest for unit tests. 

Setup Instructions: 

- clone repo 
- run `npm install` 

For tests: 
- run `npm test`

console-app.ts created to rapidly iterate on search queries and validate search results. 

To run the console app:
- run `npm run cli`